{
  "name": "recruitment-tracker-e2e",
  "private": true,
  "version": "1.0.0",
  "description": "End-to-end browser tests for the Altrium Recruitment Tracker (test-only; the app itself is plain PHP/JS with no build step).",
  "scripts": {
    "test:e2e": "playwright test",
    "test:e2e:headed": "playwright test --headed"
  },
  "devDependencies": {
    "@playwright/test": "^1.47.0"
  }
}
